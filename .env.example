# Development
DATABASE_URL=postgresql://krcrm:krcrm@localhost:5432/krcrm

# Master account (cannot be deleted or demoted by anyone)
MASTER_USERNAME=
MASTER_PASSWORD=

# Internal API Key for device service communication
INTERNAL_API_KEY=dev-secret

# Production (inside Docker — DATABASE_URL is set in docker-compose.yml)
# ORIGIN=https://yourdomain.com
